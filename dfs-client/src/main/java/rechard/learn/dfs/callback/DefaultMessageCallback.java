package rechard.learn.dfs.callback;

/**
 * @author Rechard
 **/
public class DefaultMessageCallback implements MessageCallback {

//    private FileSystemClient fsClient;
//
//    public DefaultMessageCallback(FileSystemClient fsClient) {
//        this.fsClient = fsClient;
//    }

    @Override
    public void handle(Response response) {
    }
}
